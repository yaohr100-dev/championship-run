{
  "name": "championship-run",
  "version": "1.0.0",
  "private": true,
  "description": "Championship Run — NBA roster-building game (root package.json for Railway deploy)",
  "scripts": {
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=23.4.0"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
